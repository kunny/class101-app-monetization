name: sticky_notes
description: 간단한 노트 애플리케이션
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_admob: ^0.10.3
  firebase_core: ^0.5.3
  sqflite: ^1.3.2+3
  path: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
